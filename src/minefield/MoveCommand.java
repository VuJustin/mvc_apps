package minefield;
import mvc.*;
public class MoveCommand extends Command {
Heading heading;

public MoveCommand(Model model){
    super(model);
}

public void execute(){
    //Requires minefield model
    //Minefield minefight = (Minefield)model;

    // Requires minefield model
    //minefield.move();
}
}
